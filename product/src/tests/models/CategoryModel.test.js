import { describe, it } from '@jest/global';
// import Product from '../../models/ProductModel.js';

describe('Testando alguma coisa', () => {
    it('teste interno', () => {
        const seila = 'seila to testando';
        console.log(seila);
    });
});
